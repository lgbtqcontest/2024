{
  "version": 2,
  "builds": [
    {
      "src": "dist/",
      "use": "@now/static-builder",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
